<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorAccent">@color/color_accent_night</item>

        <item name="colorAccentedSurface">@color/color_divider_light</item>
        <item name="colorIcon">@android:color/white</item>
        <item name="colorDivider">@color/color_divider_dark</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowBackground">@android:color/black</item>

        <item name="colorItemBackground">@color/color_black_light</item>
        <item name="colorItemText">@color/color_gray</item>
        <item name="colorCounter">@color/color_indicator_night</item>
        <item name ="colorIndicator">@color/color_accent_night</item>
        <item name="colorSwipeBackground">@color/color_accent_night</item>
        <item name="colorSelectUser">@color/color_accent_night</item>
        <item name="colorChipBg">@color/color_black_light</item>
        <item name="colorChipCloseIcon">@color/color_gray</item>
        <item name="colorSnackbarText">@color/color_snackbartext_night</item>

        <item name="toolbarStyle">@style/toolbar_style</item>
        <item name="snackbarStyle">@style/snackbar_style</item>
    </style>

    <style name="toolbar_style" parent="Widget.AppCompat.Toolbar">
        <item name="titleTextColor">@android:color/white</item>
        <item name="android:background">@color/color_toolbar_night</item>
    </style>
    <style name="snackbar_style" parent="Widget.MaterialComponents.Snackbar">
        <item name="android:background">@android:color/white</item>
    </style>

    <!-- Cовсем никак не сделано:
      - черная клавиатура
      - темная левая часть в swipe
      - разделитель item-ов(цвет) в списке
    -->

    <style name = "AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar"/>
    <style name = "AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light"/>

</resources>